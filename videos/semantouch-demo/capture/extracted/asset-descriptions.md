# Asset inventory

No website assets were captured. The composition is evidence-led and uses only generated typography, CSS/SVG geometry, the deterministic Semantouch cursor renderer, and machine-readable fixture/tool records produced by `scripts/record-demo-evidence.mjs`.
